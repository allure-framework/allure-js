import { it, before, beforeEach, afterEach, after } from "mocha";

before(async () => {});
beforeEach(async () => {});
afterEach(async () => {});
after(async () => {});

it("affected by four fixtures", async () => {});
