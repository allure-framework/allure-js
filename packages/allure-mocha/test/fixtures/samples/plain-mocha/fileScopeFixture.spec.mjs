import { it, before, beforeEach, afterEach, after } from "mocha";

before(async () => {});
beforeEach(async () => {});
afterEach(async () => {});
after(async () => {});

it("a test affected by four fixtures", async () => {});
