#!/usr/bin/env node
const process = require("node:process");
const allureTap = require("../dist/index").default;

allureTap(process);

// process.stdin
//   .pipe(allureTap(process))
//   // .pipe(process.stdout);

// TODO
// process.on('exit', function (status) {
//   if (status === 1) {
//     process.exit(1);
//   }
//
//   if (tapSpec.failed) {
//     process.exit(1);
//   }
// });
